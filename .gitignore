# Python bytecode / caches
__pycache__/
*.py[cod]
*$py.class

# 构建产物（真实材料的解析报告/接管清单，不入库）
parse_report.json
ai_review_manifest.json

# local RAG experiment scratch (not shippable)
spike/

# v4-P6: built runtime bundles (attached to releases, never committed)
dist/

# Persistent cross-process lock metadata created while validating workspaces.
.study_state.lock
