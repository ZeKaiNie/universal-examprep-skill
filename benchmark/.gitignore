# 运行产物：默认忽略，但放行「发布用」图表/报告/汇总（原始逐题日志 raw*.jsonl / answers / scores 仍本地保留）
results/*
!results/*.svg
!results/*.html
!results/*.md
!results/matrix/
results/matrix/*
!results/matrix/*.svg
!results/matrix/report.html
!results/matrix/summary.json
__pycache__/
*.py[cod]

# Tier 2 行为冒烟的运行产物（opt-in --llm 真跑写在这里；默认 mock/check 不写盘）
behavior_smoke/results/

# 个人配置
config.json
config_*.json

# 真实金标集：放行「仅题面」题集(_q)做公开可见的测试集；含答案 key 的完整金标本地保留（可能逐字引用讲义）
items/items.jsonl
items/items_*_full.jsonl
items/items_*_hard.jsonl

# 你的课件/作业 —— 可能含版权或隐私，绝不提交到公开仓库
materials/*
!materials/README.md

# skill 臂生成的知识库
skill_workspace/*
!skill_workspace/README.md

# 人工校准标注（可能含答案细节）
calibration/*
!calibration/README.md
!calibration/kappa_n24_summary.json
runs/ledger.jsonl
