# 第1章 栈与队列（Stack & Queue）

## 栈（Stack）
栈是一种后进先出（LIFO）的线性结构：只在同一端（称为栈顶）进行压入（push）和弹出（pop）。
典型操作：push、pop、peek。常见应用：函数调用栈、括号匹配、表达式求值。

## 队列（Queue）
队列是先进先出（FIFO）的线性结构：一端入队（enqueue），另一端出队（dequeue）。
循环队列用取模运算复用数组空间，避免假溢出。常见应用：任务调度、广度优先搜索。
