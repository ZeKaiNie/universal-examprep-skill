# 第 1 章：栈与队列 (Stack & Queue)

> 自撰示例知识库（非版权内容）。

## 栈 (Stack)
- 存取顺序：**先进后出 (LIFO)**，只在一端（栈顶）压入 `push` 与弹出 `pop`。
- 典型应用：函数调用栈、括号匹配、撤销操作。

## 队列 (Queue)
- 存取顺序：**先进先出 (FIFO)**，队尾入队 `enqueue`、队头出队 `dequeue`。
- 典型应用：任务调度、广度优先搜索 (BFS)。
