# 第 2 章：二叉树与 BST (Binary Tree & BST)

> 自撰示例知识库（非版权内容）。

## 二叉树性质
- 高度为 h 的二叉树最多有 `2^(h+1) - 1` 个节点（根高度记为 0，满二叉树时取得）。
- 第 i 层最多 `2^i` 个节点。

## 二叉搜索树 (BST)
- 性质：左子树所有键 < 根 < 右子树所有键。
- 插入：从根比较，小往左、大往右，直到空位插入。
- 例：依次插入 `[2, 1, 3]` → 根 2、左子 1、右子 3。
